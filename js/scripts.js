// business logic

function beepBoop(input){
  let outputArray = [];
  parsedInput = parseInt(input);
  if (isNaN(parsedInput)) {
    return "Enter a number, please!"
  }
  if (parsedInput === 0) {
    return "0"
  }
}

// ui logic
